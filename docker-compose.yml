services:
  selfhostable-fishing-logbook:
    build: .
    container_name: selfhostable-fishing-logbook
    ports:
      - "80:8080"
    volumes:
      - ./data:/app/data
    environment:
      HOST: 0.0.0.0
      PORT: 8080
    restart: unless-stopped
