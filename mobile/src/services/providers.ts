export async function fetchWeather(latitude: number, longitude: number) { const url = `https://api.open-meteo.com/v1/forecast?latitude=${latitude}&longitude=${longitude}&current=temperature_2m,wind_speed_10m,weather_code&timezone=auto`; const response = await fetch(url); if (!response.ok) throw new Error("Weather is unavailable"); return response.json(); }
export async function fetchMarine(latitude: number, longitude: number) { const response = await fetch(`https://marine-api.open-meteo.com/v1/marine?latitude=${latitude}&longitude=${longitude}&current=wave_height,wave_direction,wave_period`); if (!response.ok) throw new Error("Marine data is unavailable"); return response.json(); }
export async function fetchAstronomy(latitude: number, longitude: number, date: string) { const response = await fetch(`https://api.sunrisesunset.io/json?lat=${latitude}&lng=${longitude}&date=${date}`); if (!response.ok) throw new Error("Astronomy is unavailable"); return response.json(); }
export async function fetchDepth(latitude: number, longitude: number) { const geometry = encodeURIComponent(JSON.stringify({x:longitude,y:latitude,spatialReference:{wkid:4326}})); const response = await fetch(`https://edumaps.esri.ca/arcgis/rest/services/MapServices/GreatLakesBathymetry/MapServer/0/query?f=json&geometryType=esriGeometryPoint&geometry=${geometry}&inSR=4326&spatialRel=esriSpatialRelIntersects&outFields=*&returnGeometry=false`); if (!response.ok) throw new Error("Depth is unavailable"); return response.json(); }
